/**
 * Invite codes are generated in the API (they need a crypto source), but the
 * client has to render an input of the right size, so the length itself is
 * shared.
 */
export const INVITE_CODE_LENGTH = 6;
